<resources>

  <!-- Declare custom theme attributes that allow changing which styles are
       used for button bars depending on the API level.
       ?android:attr/buttonBarStyle is new as of API 11 so this is
       necessary to support previous API levels. -->
  <declare-styleable name="ButtonBarContainerTheme">
    <attr format="reference" name="metaButtonBarStyle"/>
    <attr format="reference" name="metaButtonBarButtonStyle"/>
  </declare-styleable>

  <declare-styleable name="Theme">
    <attr format="dimension" name="buttonCornerRadius"/>
    <attr format="color" name="colorRoundButtonNormal"/>
    <attr format="color" name="colorRoundButtonDisabled"/>
    <attr format="color" name="colorRoundButtonPressed"/>
  </declare-styleable>

</resources>
